defmodule Symphony do
  @moduledoc """
  Symphony — a long-running automation service that continuously reads work
  from a Linear issue tracker, creates isolated per-issue workspaces, and runs
  a Codex coding-agent session for each issue.

  See SPEC.md for the full specification.
  """
end
